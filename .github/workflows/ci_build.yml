name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: Raul6469/android-gradle-action@1.0.0
    - name: Gradle Android
      run: gradle build
    
